int addDigits(int num) {
    int rem,sum=0;
    while(num!=0)
    {
        rem=rem%num;
        sum=sum+rem;
        num=num/10;
    }
    if(sum<10)
    {
        printf("%d",sum);
    }
    else
    {
        addDigits(int sum);
    }
}
